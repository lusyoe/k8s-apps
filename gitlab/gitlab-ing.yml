apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gitlab
spec:
  rules:
  - host: git.k2.com
    http:
      paths:
      - path: /
        backend:
          serviceName: gitlab
          servicePort: http
